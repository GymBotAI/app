{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "$assets/*": ["assets/*"],
      "$api/*": ["api/*"],
      "$components/*": ["components/*"],
      "$consts/*": ["consts/*"],
      "$context": ["context"],
      "$context/*": ["context/*"],
      "$styles": ["styles"],
      "$types/*": ["types/*"],
      "$utils/*": ["utils/*"]
    }
  },
  "extends": "expo/tsconfig.base"
}
